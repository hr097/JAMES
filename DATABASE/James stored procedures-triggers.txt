
****)Stored procedures & functions:

1)Students:

getListByCourseWise('BscIT');
getListByCourseSemWise('BscIT',5);
getListByCourseSemDivWise('BscIT',5,B);

getListByInActiveStud('MscIT'); // those who are alumni and table will be in backup data base

2)Ams_api:

getListBySemesterTime(5,'08:35','09:45');

3)Faculties:

getFacByDesignation('Teching Assistant');

4)Ams_attendance_master:

getStudAttBySpid('2020049910'); // spid

******)Triggers :

1)Students:

when student completes like 10 th sem is updated to 11 th at that time  we use this trigger to delete record from here and shift to backup tb.

2)Faculties:

when faculty permenently leaves at that time  we use this trigger to delete record from here and shift to backup tb.

2)Ams_api: [DONE]

DELIMITER $$
CREATE OR REPLACE TRIGGER insert_ams_api BEFORE INSERT ON Ams_api
FOR EACH ROW
BEGIN
DECLARE sem int;
SELECT cur_semester into sem  FROM vw_students where spid = NEW.spid;
SET NEW.semester := sem;
END $$
DELIMITER ;

3) Ams_attendance_master

on deletion of records this should be moved to backup table

4)Ams_setup_course_subject_map:

on deletion of records this should be moved to backup table

5)Ams_setup_faculties_map:

on deletion of records this should be moved to backup table

6)Ams_setup_students_map:

on deletion of records this should be moved to backup table


******)Routines:

1)Ams_api 
NOTE : Event scheduler ON krvu

DELIMITER $$
CREATE EVENT `deleteAmsApiDataDaily`
  ON SCHEDULE EVERY 1 DAY STARTS '2022-09-05 03:00:00'
  ON COMPLETION PRESERVE
DO BEGIN
  TRUNCATE TABLE ams_api;
END$$
DELIMITER ;

