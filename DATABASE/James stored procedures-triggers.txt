
****)Stored procedures & functions:

1)Students:

getListByCourseWise('BscIT');
getListByCourseSemWise('BscIT',5);
getListByCourseSemDivWise('BscIT',5,B);

getListByInActiveStud('MscIT'); // those who are alumni and table will be in backup data base


2)Ams_api:

getListBySemesterTime(5,'08:35','09:45');

******)Triggers :

1)Students:

when student completes like 10 th sem is updated to 11 th at that time  we use this trigger to delete record from here and shift to backup db.


2)Ams_api: [DONE]

DELIMITER $$
CREATE OR REPLACE TRIGGER insert_ams_api BEFORE INSERT ON Ams_api
FOR EACH ROW
BEGIN
DECLARE sem int;
SELECT semester into sem  FROM shubham where spid = NEW.spid;
SET NEW.semester := sem;
END $$
DELIMITER ;

******)Routines:

1)Ams_api table daily clear at midnight 3:00 AM

