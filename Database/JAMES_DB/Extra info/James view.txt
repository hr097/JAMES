***************)views:

1)Users: 
CREATE OR REPLACE VIEW vw_users_auth AS SELECT username,password,user_type FROM Users WHERE user_access=1;

2)Students: [DONE]
CREATE OR REPLACE VIEW vw_students AS  SELECT spid,name,gender,dob,email,contact_no,Courses.course_name,joining_year,cur_semester,cur_division,cur_roll_no FROM Students,Courses WHERE Students.course_id=Courses.course_id and Students.stud_status=1;

3) Faculties:[DONE]

CREATE OR REPLACE VIEW vw_faculties AS SELECT fid,name,gender,dob,email,contact_no,joining_year,designation FROM faculties where fac_status=1;

4) Ams_setup_faculties_map: [DONE]