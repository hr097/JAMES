CREATE TABLE Ams_attendance_master(att_no BIGINT AUTO_INCREMENT,spid VARCHAR(10) NOT NULL REFERENCES Students(spid) ON UPDATE CASCADE ,ams_setup_id  INTEGER NOT NULL REFERENCES Ams_setup_course_subject_map(ams_setup_id) ON UPDATE CASCADE ON DELETE CASCADE,fid VARCHAR(10) NOT NULL REFERENCES Faculties(fid) ON UPDATE CASCADE,att_date_time DATETIME DEFAULT CURRENT_TIMESTAMP,att_status BOOLEAN NOT NULL,PRIMARY KEY(att_no));

ALTER TABLE  Ams_attendance_master ADD CONSTRAINT CHK_att_status CHECK(att_status=TRUE OR att_status=FALSE);


