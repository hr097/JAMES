create table Ams_feedback(fb_id INTEGER AUTO_INCREMENT PRIMARY KEY,email VARCHAR(256),description VARCHAR(1000),givenAt DATETIME DEFAULT CURRENT_TIMESTAMP,rating INTEGER NOT NULL);

ALTER TABLE Ams_feedback ADD CONSTRAINT FK_feedback_email FOREIGN KEY(email) REFERENCES Users(username);
